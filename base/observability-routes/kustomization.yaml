apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - httproutes.yaml

commonLabels:
  app.kubernetes.io/name: observability-routes
  app.kubernetes.io/component: gateway
  app.kubernetes.io/part-of: observability
